## Create an environment in Ruby and TDD

1. Build the environment with Dockerfile and docker-compose 
```shell
docker-compose build
```

2. Run rspec init
```shell
docker-compose run web rspec --init
```
